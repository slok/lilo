# Lilo

Lilo is an AI-generated application that helps create cross stitch patterns from images. It runs entirely in the browser, requires no backend, and exports patterns as multi-page PDFs with icons.

## Features

- Converts uploaded images into cross stitch grids
- Lets you control palette size, grid size, and rendering options
- Generates PDF patterns with symbols/icons and legends
